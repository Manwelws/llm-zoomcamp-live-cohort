Cosine similarity = v1 dot v2 / ||v1|| * ||v2||

_all-MiniLM-L6-v2_ (model in use) outputs normailzed vectors (same direction but length of 1) so we just calculate __cosine similarity__ using the dot product only (denominator)

